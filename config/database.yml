default: &defaults
  adapter: mysql2
  encoding: utf8
  reconnect: false
  pool: 5
  username: root
  password:
  host: localhost

development:
  <<: *defaults
  database: wbotelhos

test:
  <<: *defaults
  database: wbotelhos_test
