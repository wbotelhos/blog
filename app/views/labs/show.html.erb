<div class="clearfix"></div>

<article>
  <% if is_logged? %>
    <header>
      <%= link_to t('lab.edit'), edit_lab_path(@media), class: 'button green-dark edit' %>
    </header>
  <% end %>

  <div class="body">
    <%= markdown @media.body %>
  </div>
</article>

<%= render 'shared/share_it', text: "#{@media.title} - #{@media.description}" %>

<%= render 'comments/comments' %>

<%= render 'comments/form' %>
