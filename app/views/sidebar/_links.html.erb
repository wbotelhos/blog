<h2>
  <i class="icon-link icon-large"></i>
  <%= t 'link.title' %>
</h2>

<ul>
  <% links.each do |link| %>
    <li>
      <%= link_to link.name, link.url, target: '_blank' %>
      <span><%= link_to t('navigation.link.edit'), links_edit_path(link.id) %></span>
    </li>
  <% end %>
</ul>
